# ============================================
# SEPTO Worker Environment Variables
# ============================================

# Required: PostgreSQL connection string (same as main app)
DATABASE_URL=postgresql://neondb_owner:password@ep-floral-fog-ad9s7ef4-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require

# Optional: Polling interval in milliseconds (default: 5000)
POLL_INTERVAL=5000

# Optional: Maximum concurrent jobs (default: 1)
MAX_CONCURRENT=1

# Optional: Enable mock mode for testing (default: false)
# Set to 'true' to return fake data without launching browser
USE_MOCK=false

# Optional: Apify API token (for Apify actor integration)
APIFY_API_TOKEN=
